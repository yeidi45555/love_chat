APP_NAME="MVP Chat Backend (LOCAL)"
APP_ENV=local
DEBUG=true
API_PREFIX=
DATABASE_URL="URL-DB"

OPENAI_API_KEY="Key-OpenAI"

JWT_SECRET=supersecret-cambia-esto
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXP_MINUTES=30
JWT_REFRESH_DAYS=30

# Panel admin (opcional): si lo configuras, habilita /admin/* con header X-Admin-Key
# ADMIN_SECRET=tu-secreto-admin-muy-largo

# Cuotas: unidades diarias por usuario (1 token ≈ 1 unidad). Default 10000
# DAILY_UNITS_LIMIT=10000

# Storage: local | s3 (local por ahora, s3 para futuro)
# STORAGE_BACKEND=local
# UPLOAD_DIR=uploads
# STATIC_URL_PREFIX=/static